apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-db-credentials
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "0" # before the Keycloak CR (wave 1)

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore

  target:
    name: keycloak-db-credentials
    creationPolicy: Owner

  data:
    - secretKey: username
      remoteRef:
        key: rds!cluster-29835c34-07e2-40b7-ab01-1f50cc699ef4
        property: username

    - secretKey: password
      remoteRef:
        key: rds!cluster-29835c34-07e2-40b7-ab01-1f50cc699ef4
        property: password